<div class="bg-zinc-50 w-full">
  <div class="m-10">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-100 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <div id="pages" class="min-w-full">
      <%= render @pages %>
    </div>
  </div>
</div>