<!--Full Page container-->
<div class="flex flex-col md:flex-row">
  <!--First section / left section on desktop (navigation) -->
  <%= render "nav" %>

  <!--Main content stuff-->
  <%= render "main" %>
</div>